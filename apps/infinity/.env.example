# 🔧 DEVELOPMENT ENVIRONMENT CONFIGURATION
# ⚠️  Generate APP_KEY with: node ace generate:key

# === BASIC CONFIGURATION ===
TZ=UTC
PORT=3333
HOST=localhost
NODE_ENV=development
LOG_LEVEL=debug

# === SECURITY ===
# ⚠️  CRITICAL: Generate with: node ace generate:key
APP_KEY=GENERATE_WITH_node_ace_generate:key

# === SESSION ===
SESSION_DRIVER=cookie
COOKIE_SECURE=false
COOKIE_HTTP_ONLY=true
COOKIE_SAME_SITE=lax
SESSION_COOKIE_NAME=infinity_dev_session

# === DATABASE (SQLite for development) ===
DB_CONNECTION=sqlite
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=infinity
DB_PASSWORD=
DB_DATABASE=Tyfo.dev

# === REDIS (Optional in development) ===
# REDIS_HOST=127.0.0.1
# REDIS_PORT=6379
# REDIS_PASSWORD=

# === TRANSMIT (WebSockets) ===
TRANSMIT_APP_KEY=dev_transmit_key_change_in_production

# === SENTRY (Optional in development) ===
# SENTRY_DSN=
# SENTRY_ENVIRONMENT=development

# === FILE STORAGE ===
DRIVE_DISK=fs

# === VITE (Frontend) ===
VITE_APP_NAME=Infinity Game

# === TESTING ===
TEST_TMP_DIR=/tmp/infinity